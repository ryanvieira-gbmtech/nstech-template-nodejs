pre-commit:
  commands:
    "lint and format":
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc,yml}"
      run: npx @biomejs/biome check --write --no-errors-on-unmatched --files-ignore-unknown=true --colors=off --unsafe {staged_files}
      stage_fixed: true

pre-push:
  commands:
    "run all tests":
      run: npm run test
